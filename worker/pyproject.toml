[project]
name = "clipforest-worker"
version = "1.0.0"
description = "ClipForest media worker: ingest, transcription, highlight analysis, smart-reframe rendering and cleanup."
requires-python = ">=3.11"

