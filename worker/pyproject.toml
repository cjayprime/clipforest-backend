[project]
name = "cliprover-worker"
version = "1.0.0"
description = "ClipRover media worker: ingest, transcription, highlight analysis, smart-reframe rendering and cleanup."
requires-python = ">=3.11"

